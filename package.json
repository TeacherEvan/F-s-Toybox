{
  "name": "fs-toybox",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "playwright test",
    "e2e:update-snapshots": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "vitest": "^2.1.0",
    "@playwright/test": "^1.62.0"
  }
}
