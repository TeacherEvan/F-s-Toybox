Debrief — run-2026-08-26-m4 (Milestone 4: WebGL2 engine + data texture)

## 1. Executive Summary
M4 shipped TDD-style. WebGL2 fragment pipeline boots with spiral background,
64×4 RGBA32F entity data texture wired end-to-end (pack → upload → readEntity),
?seed/?t determinism hooks live, no-penguin guard enforced. All gates green.

## 2. Original Request
"proceed, then commit and push" after M3 — next open objective per plan dispatcher.

## 3. Initial State
main @ b1ddade (M3 done), clean tree.

## 4. Research
None needed — reference sections pinned by plan (VERT 431-434, renderCosmos
247-280, post-FX 397-428, paint2DFallback 473-493 of docs/concept/index.html).

## 5. Architecture
index.html owns VERT/FRAG strings + bootGL/startEngine; src/pack.js owns scene→
texture packing (slot maps per design §3.5, orbit world-position resolution);
src/scene.js now exports KIND_GPU. Uniform-cap guard aborts < 896 components.

## 6. Implementation
FRAG: hashes seeded via uSeed; renderCosmos ported with uSpiralSpeed +
uStarDensity substitutions (banned uSpin removed by design); Entity struct +
readEntity samples 64w×4h texture at y-centers .125/.375/.625/.875; smoke loop
paints white on first live entity; post chain chroma→aces→grade→gamma→vignette→grain.
JS: rAF loop packs every frame, uploads RGBA32F 64×4, sets uniforms incl.
seed/spiral/star/camera; drag=yaw/pitch, wheel=zoom; FROZEN_T freezes elapsed.

## 7. Files Changed
index.html (+~330), src/pack.js (new), src/scene.js (KIND_GPU export),
tests/no-penguin.test.js, tests/pack.test.js, tests/e2e/engine.spec.js,
tests/golden/{boot-stub-1,engine-1entity}.png

## 8. Security Review
PASS — secret scan 0 hits; no new network surface (same-origin fetch only).

## 9. Validation
vitest 37/37 EXIT=0 · playwright 7/7 EXIT=0 (boot, panel ×3, engine ×3).

## 10. Playwright
Uniform console-log poll assertion; canvas luma stats via drawImage/getImageData
(preserveDrawingBuffer:true); empty-vs-galaxy mean-luma gap > 0.3 asserted;
golden captured under ?seed=42&t=0 (deterministic).

## 11. Consistency Review
Plan ↔ design ↔ code agree except 3 recorded deviations (TRACEABILITY §Deviations),
all improving correctness vs plan-text residue. consistency_attempts=0.

## 12. Retry/Failure History
RED phase intentional (pack collect-fail + 3 E2E red). One test-authoring fix:
moon column indexing (base offset 16, not 0) — test bug, not product bug.

## 13. Git Summary
4b500ad RED · 1051226 feat(engine) · chore(audit) this commit. Push follows user request.

## 14. Remaining Work
M5 galaxy kind (first real renderEntity), M6-M13. Child parentSlot lookup deferred
to M7 per plan (readEntity returns -1 placeholder). Camera shift-drag select (M3+
polish) still open.

## 15. Final Recommendation
READY WITH WARNINGS — warning: Node v24 vs engines >=22 <23 persists (Risk #11).

## 16. Agent Handoff
Next: M5 Task 5.1 renderGalaxy() port + hsv2rgb helper + golden galaxy spec.
Keep sign-kind packing contract; per-kind branches dispatch on e.kind int.

## 17. Audit Metadata
run_id run-2026-08-26-m4 · verification_attempts=1 (first-pass green after RED)
· final_status=READY WITH WARNINGS
